<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"
        tools:node="merge" />

    <application>
        <activity
            android:name=".DeveloperPanelActivity"
            android:exported="true"
            android:icon="@drawable/ic_launcher_developer_panel_foreground"
            android:label="@string/title_activity_developer_panel"
            android:taskAffinity="com.behnawwm.developerpanelplayground.DeveloperPanelActivity"
            android:theme="@style/Theme.DeveloperPanelPlayground"
            tools:node="merge">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>